import { <%= pascalCase(pageName) %>Page, <%= pascalCase(pageName) %>PageContainer } from './<%= pageName %>'

export {
    <%= pascalCase(pageName) %>Page,
    <%= pascalCase(pageName) %>PageContainer
}