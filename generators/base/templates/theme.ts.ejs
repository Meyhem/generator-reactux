import { createGlobalStyle } from 'styled-components'

export const theme = {
  colors: {
    primary: '#000833',
    secondary: '#000',
    info: '#000',
    success: '#000',
    warning: '#000',
    danger: '#000',

    text: '#b4c0ff',
  },
  gradients: {
    primary:
      'linear-gradient(180deg, rgba(0,8,51,1) 0%, rgba(0,17,107,1) 100%)',
    primaryMirrorX:
      'linear-gradient(180deg, rgba(0,17,107,1) 0%, rgba(0,8,51,1) 100%)',
  },
  fontSizes: {
    tiny: '.2rem',
    small: '.5rem',
    normal: '1rem',
    big: '2rem',
    mega: '5rem',
  },
  breakpoints: {
    xs: 320,
    sm: 576,
    md: 768,
    lg: 992,
    xl: 1200,
  },
}

export type Breakpoint = keyof typeof theme.breakpoints

export function brk(min: Breakpoint) {
  return (contentCss: any) => `
    @media (min-width: ${theme.breakpoints[min]}px) { ${contentCss} }
  `
}

export function brkbt(min: Breakpoint | null, max: Breakpoint) {
  return (contentCss: any) => `
    @media (min-width: ${min ? theme.breakpoints[min] : 0}px) and (max-width: ${
    theme.breakpoints[max]
  }px) { ${contentCss} }
  `
}

export const GlobalStyle = createGlobalStyle`
  body {
    background: ${({ theme }) => theme.colors.primary}
  }
`
